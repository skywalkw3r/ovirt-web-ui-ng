# See http://help.github.com/ignore-files/ for more about ignoring files.

# dependencies
node_modules
.venv/

# lab — user-specific copies (secrets stay in env, but these carry lab details)
lab/ansible/vars.yml
lab/ansible/inventory.ini

# deploy — the LIVE OpenShift manifests (real config.js, routes, nginx engine
# map) carry internal Keysight hostnames; this repo is public, so they stay
# local-only. The sanitized template lives in packaging/openshift/.
/deploy/

# testing
coverage

# production
build

# misc
.DS_Store
npm-debug.log
.idea
.vscode/
.env*

# automake.sh / automake / autoconfig generated files
Makefile
Makefile.in
aclocal.m4
autom4te.cache/
config.log
config.status
configure
install-sh
missing
ovirt-web-ui.spec
tmp.repos/
src/version.js
/ovirt-web-ui-*.tar.gz

# intl related
/extra

# mock_runner
.bash_history
.node-gyp/
exported-artifacts/
rpmbuild/
mocker-*.cfg

# Not autogenerated!
\!.copr/Makefile

# autogenerated configure script
configure~

# Playwright e2e artifacts
test-results/
